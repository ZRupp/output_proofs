services:
  benchmark:
    build:
      context: .
      args:
        - BASE_IMAGE=${BASE_IMAGE:-rocm/pytorch:rocm7.1_ubuntu22.04_py3.10_pytorch_release_2.9.1}
    environment:
      - INFERENCE_MODEL=${INFERENCE_MODEL:-meta-llama/Llama-3.2-3B-Instruct}
      - FORMALIZER_MODEL=${FORMALIZER_MODEL:-Goedel-LM/Goedel-Formalizer-V2-8B}
      - HF_TOKEN=${HF_TOKEN:-}
      - DEVICE=${DEVICE:-auto}
      - HSA_OVERRIDE_GFX_VERSION=11.0.0
    volumes:
      - ./results:/app/results
      - ~/.cache/huggingface:/root/.cache/huggingface
    devices:
      - /dev/kfd:/dev/kfd
      - /dev/dri:/dev/dri
    security_opt:
      - seccomp=unconfined
    command: ["--quantize", "4"]

  # CPU-only profile for testing/dry-run without GPU
  # Start with: docker compose --profile cpu-only up benchmark-cpu
  benchmark-cpu:
    profiles:
      - cpu-only
    build:
      context: .
      args:
        - BASE_IMAGE=${BASE_IMAGE:-rocm/pytorch:rocm7.1_ubuntu22.04_py3.10_pytorch_release_2.9.1}
    environment:
      - INFERENCE_MODEL=${INFERENCE_MODEL:-meta-llama/Llama-3.2-3B-Instruct}
      - FORMALIZER_MODEL=${FORMALIZER_MODEL:-Goedel-LM/Goedel-Formalizer-V2-8B}
      - HF_TOKEN=${HF_TOKEN:-}
      - DEVICE=cpu
    volumes:
      - ./results:/app/results
      - ~/.cache/huggingface:/root/.cache/huggingface
